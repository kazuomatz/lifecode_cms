<!---
#  new.html.erb
#  Generated by LifeCodeCMS Scaffold Generator
-->
<%= content_for :breadcrumb do %>
  <% items = [
      {url: '/admin', title: Settings.form.label.home},
      {url: "#{admin_login_histories_path}", title: "#{LoginHistory.label}#{Settings.form.label.list}"},
      {title: "#{LoginHistory.label}#{Settings.form.label.create}"},
  ]
  %>
  <%= render partial: 'layouts/breadcrumb', locals: {items: items} %>
<% end %>
<div class="container-fluid base">
  <div>
    <%= form_for @login_history, html: {class: 'form'} do |form| %>
      <div class="row">
        <div class="col-sm-12">
          <h1 class="func-title"><%= LoginHistory.label %><%= Settings.form.label.create %></h1>
          <div class="card">
            <div class="card-header">
              <strong><i class="<%= LoginHistory.icon %>"></i>　<%= LoginHistory.label %><%= Settings.form.label.create %>
              </strong>
            </div>
            <div class="card-body">
              <div class="row">
                <div class="col-sm-12">
                  <%= render partial: 'form', locals: {form: form, login_history: @login_history} %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="regist-buttons-wrapper">
        <div class="card-footer regist-buttons">
          <button type="submit" class="btn btn-primary btn-lg">
            <i class="far fa-check-circle"></i> <%= @login_history.new_record? ? Settings.form.label.create : Settings.form.label.update %>
          </button>
          <a href="<%= (session[params[:controller]].present? && session[params[:controller]]['return_path']) || admin_login_histories_path %>" class="btn  btn-lg btn-default">
            <%= Settings.form.label.cancel %>
          </a>
        </div>
      </div>
    <% end -%>
  </div>
</div>

<%- content_for :add_js do %>
  <script>
    var weekStrings = JSON.parse('<%= raw I18n.t("date.abbr_day_names").to_json %>');
    $(function () {
      new FormManager().init();
    });
  </script>
<%- end %>