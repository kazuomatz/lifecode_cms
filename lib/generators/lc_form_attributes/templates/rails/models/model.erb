<%#- encoding: UTF-8 -%>
#  Admin::<%= @file_name.singularize.camelize %>
#  Generated by LifeCodeCMS FormAttributes Generator

<%- model_name = @file_name.singularize.camelize
    model = class_eval model_name
    search_columns = model.search_columns
-%>
module Admin
  class <%= model_name %> < <%= model_name %>
    class << self
      def search(params)
        objects = self.all
        <%- search_columns.each do | column | -%>
        if params[:<%= column %>].present?
          objects = objects.where(<%=column%>: params[:<%= column %>])
        end
        <%- end -%>
        objects
      end
    end
  end
end