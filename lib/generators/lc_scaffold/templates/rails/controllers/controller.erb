#  Admin::<%= @file_name.pluralize.camelize %>Controller
#  Generated by LifeCodeCMS Scaffold Generator

module Admin
  class <%= @file_name.pluralize.camelize %>Controller < Admin::BaseController
    layout 'application'
    before_action  :authenticate_user!
    before_action  :check_execute_permission
    before_action  :permit_params, only: %i[create update]
    before_action  :set_referer, only: [:edit, :new]

    def index
      if request.xhr?
        <%= @file_name.pluralize.underscore %> = Admin::<%= @file_name.singularize.camelize %>.search(params).page(params[:page]).per(20)
        pagination = view_context.paginate(<%= @file_name.pluralize.underscore %>, remote: true, window: 1)
        content = render_to_string(partial: 'list.html', locals: { search_params: merge_search_params, <%= @file_name.pluralize.underscore %>: <%= @file_name.pluralize.underscore %>  })
        render json: { pagination: pagination, content: content, page: params[:page] || 1, status: 'OK' }
      else
        render
      end
    end

    def new
      flash[:alert] = nil
      @<%= @file_name.singularize.underscore %> = Admin::<%= @file_name.singularize.camelize %>.new
    end

    def edit
      @<%= @file_name.singularize.underscore %> = Admin::<%= @file_name.singularize.camelize %>.where(id: params[:id]).first
      rescue_404 if @<%= @file_name.singularize.underscore  %>.blank?
    end

    def create
      @<%= @file_name.singularize.underscore %> = Admin::<%= @file_name.singularize.camelize %>.new(@attr)
      @<%= @file_name.singularize.underscore %>.save!
      redirect_to session[params[:controller]] && session[params[:controller]]['return_path'] || admin_<%= @file_name.pluralize.underscore %>_path
    end

    def update
      @<%= @file_name.singularize.underscore %> = Admin::<%= @file_name.singularize.camelize %>.where(id: params[:id]).first
      if @<%= @file_name.singularize.underscore %>.present?
        begin
          if @<%= @file_name.singularize.underscore %>.update(@attr)
            redirect_to session[params[:controller]] && session[params[:controller]]['return_path'] || admin_<%= @file_name.pluralize.underscore %>_path
          else
            flash[:alert] = @<%= @file_name.singularize.underscore %>.errors.message
            render template: '<%= @file_name.pluralize.underscore %>/edit'
          end
        rescue StandardError => e
          rescue_500(e)
        end
      else
        rescue_404
      end
    end

    private

    def permit_params
      @attr = params.require('admin_<%= @file_name.singularize.underscore %>').permit(
        <%= class_eval(@file_name.singularize.camelize).form_attributes.map{ |a| ':' + a[:name] }.join(",") %>
      )
    end
  end
end
