<%#- encoding: UTF-8 -%>
<!---
#  new.html.erb
#  Generated by LifeCodeCMS Scaffold Generator
-->
<%%= content_for :breadcrumb do %>
  <%% items = [
    {url: '/admin', title: Settings.form.label.home},
    {url: "#{admin_<%= var_models %>_path}", title: "#{<%= model.name %>.label}#{Settings.form.label.list}"},
    {title: "#{<%= model.name %>.label}#{Settings.form.label.create}"},
  ]
  %>
  <%%= render partial: 'layouts/breadcrumb', locals: {items: items} %>
<%% end %>
<div class="container-fluid base">
  <div>
    <%%= form_for @<%= var_model %>, html: {class: 'form'} do |form| %>
    <div class="row">
      <div class="col-sm-12">
        <h1 class="func-title"><$$= <%= model.name %>.label $$><%%= Settings.form.label.create %></h1>
        <div class="card">
          <div class="card-header">
            <strong><i class="<$$= <%= model.name %>.icon $$>"></i>　<$$= <%= model.name %>.label $$><%%= Settings.form.label.create %></strong>
          </div>
          <div class="card-body">
            <div class="row">
              <div class="col-sm-12">
                <%%= render partial: 'form', locals: {form: form, <%= var_model %>: @<%= var_model %>} %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="regist-buttons-wrapper">
      <div class="card-footer regist-buttons">
        <button type="submit" class="btn btn-primary btn-lg">
          <i class="far fa-check-circle"></i> <%%= @<%= var_model %>.new_record? ? Settings.form.label.create : Settings.form.label.update %>
        </button>
        <a href="<%%= (session[params[:controller]].present? && session[params[:controller]]['return_path']) || admin_<%= var_models %>_path %>" class="btn  btn-lg btn-default">
          <%%= Settings.form.label.cancel %>
        </a>
      </div>
    </div>
    <%% end -%>
  </div>
</div>

<%%- content_for :add_js do %>
<script>
  var weekStrings = JSON.parse('<$$= raw I18n.t("date.abbr_day_names").to_json $$>');
  $(function () {
    new FormManager().init();
  });
</script>
<%%- end %>